# Bad Checklist Entry Example

```markdown
- [ ] Continue fixing docs and scripts everywhere as needed.
```

## Why this is bad

- Uses unbounded wording (`continue`, `everywhere`) that creates task churn.
- Provides no file scope, dependency context, or acceptance criteria.
- Cannot be cleanly closed, so agents will annotate progress instead of completing work.
