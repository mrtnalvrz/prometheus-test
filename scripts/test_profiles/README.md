# scripts/test_profiles/

Plain-text test profile definitions for `scripts/run_tests.py`.

## Contents

- `baseline.txt`: Default curated profile listing stable test targets for wrapper-driven execution.

Each profile should contain one pytest path/node selector per line (comments allowed with `#`).
